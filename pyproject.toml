[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rasp-lib"
version = "0.1.0"
dependencies = [
  "flask",
  "google-cloud-logging",
  "pytz"
]